# dependencies (matches at any depth, e.g. backend/node_modules, frontend/node_modules)
node_modules/

# build output (matches at any depth)
dist/
claude_design/

# env
.env
.env.*.local

# logs
*.log
npm-debug.log*

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# personal CV document — never commit the source file, only the derived seed data
cv/*
!cv/README.md

# personal avatar photo — never commit the source file, only the derived seed data
avatar/*
!avatar/README.md
