# URL of the backend's GraphQL endpoint. Defaults to http://localhost:3000/graphql
# (matching `docker compose up` / `npm run start:dev` on localhost) if unset.
VITE_API_URL="http://localhost:3000/graphql"
